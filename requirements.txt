# Source-of-truth for runtime deps. Keep version ranges loose here
# (`>=X.Y`) and let `uv pip compile` pin transitives into requirements.lock.
#
# When you edit this file, regenerate the lock:
#   uv pip compile requirements.txt --python-version 3.10 -o requirements.lock
#
# Commit both files together. The Dockerfile and `pip install -e .` both
# read requirements.lock, so an out-of-date lock won't be picked up by
# prod or by `pip install -e ".[dev]"`.

flask>=3.0
gunicorn>=22.0
requests>=2.31
beautifulsoup4>=4.12
civic-scraper>=1.0
pdfplumber>=0.10
psycopg2-binary>=2.9
python-dotenv>=1.0
anthropic>=0.39
pydantic>=2.6
apscheduler>=3.10
numpy>=1.26,<2
opencv-python-headless==4.10.0.84
pytesseract==0.3.13
