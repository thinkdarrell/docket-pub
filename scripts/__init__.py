"""One-shot data scripts (backfills, imports, eval runs).

Modules here are intended to be invoked directly via
``python scripts/<name>.py`` — the empty ``__init__`` exists so the
modules are also importable from tests for unit/integration coverage.
"""
